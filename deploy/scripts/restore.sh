#!/usr/bin/env bash
# -----------------------------------------------------------------------------
# TASK BRIEF — Week 4 — write it, then ACTUALLY RUN IT
# -----------------------------------------------------------------------------
#
# REQUIREMENTS
#   - take the latest dump (or one passed as an argument)
#   - restore into an EMPTY database, never over production
#   - verify: count rows in key tables and compare
#   - measure and print the duration — that number is the RTO and it goes
#     into the weekly report
#
# DoD: this script has been run to success at least once, with evidence at
# docs/progress/evidence/week-04/restore-drill.txt
set -euo pipefail

echo "TODO Week 4" >&2
exit 1
