# -----------------------------------------------------------------------------
# TASK BRIEF — Week 2
# Agent: read agent/rules/30-docker-deploy.md. Respect the RAM constraint:
# the development laptop may have only 8 GB.
# -----------------------------------------------------------------------------
#
# PROFILES (mandatory — never start everything at once)
#   core    : api, postgres, redis   (web ditunda ke Minggu 4 - Cloudflare Pages)
#   storage : minio
#   obs     : otel-collector, prometheus, grafana, loki, tempo
#
# EVERY SERVICE MUST HAVE
#   - healthcheck, and depends_on with condition: service_healthy
#   - a memory limit (non-negotiable on this hardware)
#   - restart policy
#   - configuration via env_file, never hardcoded
#
# QUESTIONS THE HUMAN MUST BE ABLE TO ANSWER AFTERWARDS
#   - Why can `api` reach `postgres` by service name, and why is `localhost`
#     inside a container not the laptop?
#   - Bind mount vs named volume: which for Postgres data, which for hot
#     reload, and why?
#   - Why is depends_on insufficient without a healthcheck?
#
# EVIDENCE REQUIRED
#   docs/progress/evidence/week-02/compose-ps-healthy.txt
#   docs/progress/evidence/week-02/oom-kill-demo.txt

# TODO Week 2
# -----------------------------------------------------------------------------

name: campus-lms

services:
  api:
    profiles: [core]
    build:
      context: ../../apps/api
    image: campus-lms-api:dev
    env_file:
      - ../../.env
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    mem_limit: 128m
    restart: unless-stopped
    # The production image supplies its own /api -healthcheck probe.

  postgres:
    profiles: [core]
    image: postgres:16.14-alpine3.23
    env_file:
      - ../../.env
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    mem_limit: 512m
    restart: unless-stopped

  redis:
    profiles: [core]
    image: redis:7.4.10-alpine3.21
    env_file:
      - ../../.env
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    mem_limit: 128m
    restart: unless-stopped

volumes:
  postgres-data:
